import React from "react";
import ReactCountryFlag from "react-country-flag";

const CountrySelectionDropdown = () => {
  return <ReactCountryFlag countryCode="CA" svg style={{}} />;
};

export default CountrySelectionDropdown;
